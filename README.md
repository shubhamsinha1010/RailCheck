# RailCheck

Calibrated safety gate for LLM and agent outputs.

RailCheck sits after any model or agent step and returns typed System One decisions (`choice`, `score`, `noul`) with confidence bands — allow, rewrite, block, or human review — before a response reaches the user or a tool executes.

## Status

Scaffolding in progress. See feature branches for active work.
